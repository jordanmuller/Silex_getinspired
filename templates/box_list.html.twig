{% extends "layout.html.twig" %}

{% block title 'Box - ' %}

{# {% block stylesheets %}
    {{ parent() }} {# reprend le contenu du bloc parent #}
   {# <link href="{{asset('css/admin/box_list_admin.css')}}" rel="stylesheet" type="text/css" />
{% endblock %} #}

{% block content %}
    <div class="container">
        <h1 style="text-align:center;">Découvrez nos Box</h1><hr/>
        <br>
        <div class="col-sm-9 col-sm-offset-2">
            <div class="row">              
                {% set compteur = 0 %}
                {% for box in boxes %} 
                    {% if compteur % 2 == 0 %}                    
                        </div><div class="row">
                    {% endif %}
                    <div class="col-sm-5">
                        <div class="panel panel-default panel-box">
                            <div class="panel-heading"><img src="{{ asset('img/cart.png') }}" class="img-responsive img-box" /></div>
                            <div class="panel-body">
                                <div class="row">
                                    <div class="col-sm-6">
                                        <p><strong>{{ box.title }}</strong></p>
                                    </div>
                                    <div class="col-sm-6">
                                        <p class="text-right"><strong>{{ box.price }}€</strong></p>
                                    </div> 
                                </div>
                                <div class="row">                                    
                                    <div class="col-sm-6">
                                        <a href="{{ path('box_detail', {id: box.id}) }}" class="pull-left"><span class="glyphicon glyphicon-search" style="font-size: 1em;"></span> Voir la box</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                        
                {% set compteur = compteur + 1 %}
                {% endfor %}
              
                
            </div><!-- /.row -->
        </div><!-- /.col-sm-9 -->
    </div>
{% endblock  %}
