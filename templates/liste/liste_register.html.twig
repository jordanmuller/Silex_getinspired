{% extends "layout.html.twig" %}

{% block title 'Listes - Get Inspired' %}

{% block stylesheets %}
    {{ parent() }} {# reprend le contenu du bloc parent #}
{#    <link href="{{asset('css/movies.css')}}" rel="stylesheet" type="text/css" />#}
{% endblock %}

{% block content %}
    <div class="container">
        {% if liste.id_list is null %}
        <h1 style="text-align: center;">Ajouter une liste</h1><hr/>
        {% else %}
            <h1 style="text-align: center;">Modification de la liste</h1><hr/>
        {% endif %}
        
            <div class="row">
                <div class="col-sm-offset-3 col-sm-6">
                    
                    <form method="post" enctype="multipart/form-data">
                        <div class="form-group">
                            <label>Titre</label>
                            <input type="text" id="title" name="title" value="{{ liste.title }}" class="form-control">
                        </div>

                        <div class="form-group">
                            <label>Description</label>
                            <textarea name="description" id="description" rows="7" cols="66" resize="vertical">{{ liste.description }}</textarea>
                        </div>
                        
                        {% if liste.id_list is not null %}
                            <div class="form-group">
                                <label>Image actuelle</label>

                                 <img src="{{asset('photos/' ~ liste.picture)}}" class="img-responsive">

                                 <input type="hidden" name="old_picture" value="{{liste.picture}}" />
                            </div>
                        {% endif %}
                       
                        
                        <div class="form-group">
                            <label>Affiche</label>
                            <input type="file" id="picture" name="picture" class="form-control">
                        </div>
                        
                        <div class="form-group">
                            <label>Film 1</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[0] is defined and movie.id == listeMovies[0].id %}selected{% endif %}>{{ movie.title }}</option> 
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 2</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[1] is defined and movie.id == listeMovies[1].id %}selected{% endif %}>{{ movie.title }}</option> 
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 3</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[2] is defined and movie.id == listeMovies[2].id %}selected{% endif %}>{{ movie.title }}</option> 
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 4</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[3] is defined and movie.id == listeMovies[3].id %}selected{% endif %}>{{ movie.title }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 5</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[4] is defined and movie.id == listeMovies[4].id %}selected{% endif %}>{{ movie.title }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 6</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[5] is defined and movie.id == listeMovies[5].id %}selected{% endif %}>{{ movie.title }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 7</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[6] is defined and movie.id == listeMovies[6].id %}selected{% endif %}>{{ movie.title }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 8</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[7] is defined and movie.id == listeMovies[7].id %}selected{% endif %}>{{ movie.title }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 9</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[8] is defined and movie.id == listeMovies[8].id %}selected{% endif %}>{{ movie.title }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label>Film 10</label>
                            <select id="movie" name="movie[]" class="form-control">
                                <option value="" disabled selected>Choisissez un film</option>
                                {% for movie in movies %}
                                    <option value="{{ movie.id }}" {% if listeMovies[9] is defined and movie.id == listeMovies[9].id %}selected{% endif %}>{{ movie.title }}</option>
                                {% endfor %}
                            </select>
                        </div>

                        <button type="submit" class="btn btn-primary form-control"><span class="glyphicon glyphicon-off" aria-hidden="true"></span> Enregistrer</button>                     
        
                    </form>
                </div>  
            </div>
        </div>
{% endblock %}
